<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Electric Shutter App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElectricShutterApp">

        <!-- Activity اصلی (Launcher) -->
        <activity
            android:name=".ui.main.MainScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- سایر Activity ها -->
        <activity
            android:name=".ui.baseprice.BasePriceScreen"
            android:exported="false" />

        <activity
            android:name=".ui.reports.ReportsPageScreen"
            android:exported="false" />

    </application>

</manifest>
